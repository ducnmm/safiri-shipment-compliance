import type { FastifyInstance } from 'fastify';

/**
 * All shipment-related endpoints. Fleshed out in later steps; registered as a
 * Fastify plugin so the app wiring stays stable.
 */
export async function shipmentRoutes(_app: FastifyInstance): Promise<void> {
  // Endpoints added in subsequent steps.
}
